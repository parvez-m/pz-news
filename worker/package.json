{
  "name": "pz-news-worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply pz-news-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply pz-news-db --remote"
  },
  "dependencies": {
    "hono": "^4.7.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250310.0",
    "typescript": "^5",
    "wrangler": "^4.0.0"
  }
}
